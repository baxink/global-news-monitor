{
  "name": "global-news-monitor-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute global-news-monitor-db --file=../db/schema.sql --remote",
    "db:seed": "wrangler d1 execute global-news-monitor-db --command=\"INSERT OR REPLACE INTO media_sources SELECT * FROM json_each(readfile('../shared/media-sources.json'))\" --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240405.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.40.0"
  }
}
