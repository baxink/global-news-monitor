{
  "name": "nature-daily",
  "version": "1.0.0",
  "private": true,
  "description": "Nature 每日一篇静态日报",
  "scripts": {
    "dev:worker": "cd worker && npm run dev",
    "deploy:worker": "cd worker && npm run deploy",
    "db:init": "cd worker && npm run db:init",
    "db:seed": "cd worker && npm run db:seed"
  }
}
